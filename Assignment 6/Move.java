import java.io.*;
import java.util.*;
class Move extends LinkedList
{
    static Scanner scr=new Scanner (System.in);
    //insertion
    
    public static void main(final String[] args) 
    {
        LinkedList l1= new  LinkedList();
        input(l1);
        print(l1);
        int ind=0;
        Node curr = l1.head;
        while (curr)
        while (gin++!=r)
        {
            insertAtLast(p, curr.data);
            curr= curr.next;
            
        }
        reverse(p);
        Node z= p.head;
        while(z.next!=null)
        z=z.next;
        z.next=curr;
        print(p);
    }

}